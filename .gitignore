# ─── Device-local state (never sync) ───────────────────────────────────────────
sessions/
npm/
git/
auth.json
trust.json
extensions/herdr-agent-state.ts

# ─── Machine-local MCP & peripheral config ────────────────────────────────────
mcp.json
mcp-cache.json

# ─── OS junk ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

models-store.json

# ─── Per-device mirror (two machines diverge; BOOTSTRAP.md embedded copy is canonical) ───
settings.json
pi-cache-optimizer-stats.json
